---
date: 2026-09-12
status: unverified
status_note: 進行中
一句話: 崩潰隔離、安裝程式、更新檢查都達標，只剩發布前完整跑一輪相容性測試
相關:
  - "[[Phase 0-4 已完成]]"
  - "[[Phase 6 跨平台移植]]"
  - "[[Phase 5 剩下的（發布前必做）]]"
  - "[[進度快照]]"
  - "[[風險清單與對策]]"
---
# Phase 5 — 穩定性、相容性、發佈

> **這是目前進行中的階段**，項目會陸續打勾。已完成的五個階段在
> [[Phase 0-4 已完成]]。
>
> `[~]` = 決定不做／擋住，`[?]` = 尚未決定。

- [x] 相容性測試矩陣（[[輸入法開發的一般門檻]]點名的雷區）：清單建立於
      [[相容性測試清單]]（12 個宿主 × 21 項核心迴圈＋整機一次性項目）。
      **首輪抽測通過**——記憶體與崩潰隔離見下；完整一輪留到發布前跑，
      拿安裝程式裝出來的版本測（簽章後來決定第一版不做）
- [x] 崩潰隔離：核心引擎 panic 不得帶垮宿主程式（catch_unwind + 降級為
      passthrough）。**三項實測全過**（2026-09-02，見 [[崩潰隔離：panic 不得帶垮宿主]]），測試工具本身寫錯過
      一次、把記事本帶走——那次意外證明了 unwinding 穿過 `extern "system"`
      真的會 abort 宿主
- [x] **安裝程式**（Inno Setup）：**三個階段全部實測通過**——首次安裝、
      升級（DLL 被宿主載著）、反安裝（註冊全清、資料夾整個消失）。
      見 [[安裝程式：三階段實測全過，data_dir 在 Program Files 會靜默失效]]。裝到 `C:/Program Files/Tsunagi IME`，
      19.5MB，中日英三語精靈
- [x] **LICENSE 全文**：GPL-3.0-or-later，從 gnu.org 逐字下載
- [x] **字型嵌入**（[[提示視窗字型：商業字型條款擋下，改嵌 OFL 字型的 subset]]）：提示視窗的六個標籤字
      改用 subset 的內嵌字型（3,972 bytes），不再依賴對方電腦裝了什麼。
      **原本打算用的那套商業字型授權不允許散布**（限兩台裝置、不得轉讓），
      改用 SIL OFL 的來源
- [~] **GitHub Actions**：**先不做**。它唯一的硬性理由是「SignPath 走 CI
      管線簽」，而簽章已決定第一版先不處理。`build-installer.ps1` 本來就
      寫成無人值守，之後要接隨時能接
- [x] **更新檢查**（[[自動更新：只做「檢查並提示」，不做背景替換]]）：設定頁查
      GitHub Releases 版本號並提示。**2026-09-14 實作完並在 Windows 實測通過**
      （已是最新、沒網路、關掉自動檢查、不閃黑窗四條）
- [ ] **發布前完整跑一輪相容性測試**：清單已備好，要拿安裝程式裝出來的版本測
      （第一版不簽章，見下），測的才是使用者真的會拿到的東西。macOS 另有一份
      [[相容性測試清單_macOS]]，同樣還沒整份跑過
- [x] ~~**改寫 commit 身分**~~ → **改走無歷史快照**（2026-09-04，
      [[公開前的隱私掃描：歷史乾淨，改寫身分後來改走無歷史快照]]）：發布要用另一個帳號，
      原計畫用 `git filter-repo` 統一整段歷史的 author；後來決定**不改寫歷史**，
      公開 repo 只放 `tools/publish-snapshot.ps1`（macOS 版 `.sh`）產的快照，
      發版時跑一次、push 由使用者自己執行
- [?] **ARM64**：**沒有測試機**，發布要不要出尚未決定。Windows on ARM
      能用 x64 模擬跑程式，但 TSF 是系統元件，x64 的 IME 在 ARM64 上能
      服務哪些宿主**沒有查證過**——盲發比不發更糟
- [~] **Code signing**：**第一版先不簽**（2026-09-03 決定）。先把東西放
      出去看有沒有人要用，再投資發布工程。觸發重新評估的條件寫在
      [[Code signing：第一版先不簽，公開後再申請開源免費簽章]]
- [x] 效能目標：按鍵到候選更新 < 16ms（p99）**已達標**（10.5ms）；
      常駐記憶體 < 50MB **已達標**（232MB → **23MB**）。二進位版面
      ＋ mmap，見 [[二進位詞庫與 mmap：常駐 232MB → 23MB]]
