---
原編號: "2.52.2"
date: 2026-09-06
status: done
一句話: "core 23800 行幾乎不動，platform/windows 11700 行全部重寫，其中約 1900 行純邏輯可上搬 core"
相關:
  - "[[純邏輯上搬 core：slide／width_bar／theme 搬完，keymap 另開一篇]]"
---

# 移植前的程式碼盤點：平台層 11700 行重寫，約 1900 行可上搬 core

| 層 | 規模 | 平台相依 | 移植做法 |
|---|---|---|---|
| `core/` | 23,800 行 | 2 處（見上） | 改路徑推導，其他不動 |
| `platform/windows/` | 11,700 行 | 大半是 TSF／COM／D2D | 全部重寫成 IMK 版 |
| 其中純邏輯、可上搬到 core | 約 1,900 行 | `keymap` 的 `Mode`／`Action`／`CtrlTap`／`Combo`、`theme`、`slide`、`width_bar` | **搬到 core，兩平台共用**（§4.10 早就寫了 `Theme` 要搬） |
| `settings/` | 2,550 行 | 5 個檔用 `windows` crate（`font_dialog`、`image_dialog`、`image_load`、`preview_font`、`main` 的一小段） | eframe 本身跨平台，只換那五個檔 |

「純邏輯上搬」**現在在 Windows 上就能做、能用既有測試驗證**，是唯一不
需要 Mac 就能推進的實作。屬於拆檔重構，照 CLAUDE.md 的規範要先提案。
